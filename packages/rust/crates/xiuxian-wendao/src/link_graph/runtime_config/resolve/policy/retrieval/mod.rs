//! `link_graph::runtime_config::resolve::policy::retrieval` owns Wendao resolve policy retrieval behavior.

mod base;
mod semantic_ignition;

pub(crate) use base::resolve_link_graph_retrieval_policy_runtime;
