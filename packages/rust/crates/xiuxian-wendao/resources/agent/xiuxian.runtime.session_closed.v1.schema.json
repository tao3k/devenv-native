{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xiuxian.runtime.session_closed.v1.schema.json",
  "title": "Xiuxian Runtime session closed (DELETE tool-runtime session) v1",
  "description": "Canonical shape for tool-runtime Streamable HTTP session close response. Returned when the client closes the session.",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "message"],
  "properties": {
    "status": {
      "type": "string",
      "description": "Status indicator (e.g. ok)."
    },
    "message": {
      "type": "string",
      "description": "Human-readable message (e.g. Session closed)."
    }
  }
}
