[package]
name = "xiuxian-wendao-server"
version.workspace = true
edition.workspace = true
description = "Flight and gRPC transport boundary for Wendao"
authors.workspace = true
license.workspace = true
publish = false

[features]
default = []
performance = ["transport"]
transport = [
  "dep:arrow-array",
  "dep:arrow-flight",
  "dep:arrow-schema",
  "dep:async-trait",
  "dep:base64",
  "dep:futures",
  "dep:tokio",
  "dep:tokio-stream",
  "dep:tonic",
]

[[bench]]
name = "wendao_transport_performance"
harness = false
required-features = ["performance"]

[[example]]
name = "attachment_flight_contract_server"
required-features = ["transport"]

[dependencies]
arrow-array = { workspace = true, optional = true }
arrow-flight = { workspace = true, optional = true }
arrow-schema = { workspace = true, optional = true }
async-trait = { workspace = true, optional = true }
base64 = { workspace = true, optional = true }
futures = { workspace = true, optional = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true, optional = true }
tokio-stream = { workspace = true, optional = true }
tonic = { workspace = true, optional = true }

[dev-dependencies]
criterion = { workspace = true }
rust-lang-project-harness = { workspace = true }
toml = { workspace = true }

[lints]
workspace = true
