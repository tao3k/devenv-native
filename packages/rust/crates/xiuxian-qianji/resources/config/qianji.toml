# packages/rust/crates/xiuxian-qianji/resources/config/qianji.toml
# System defaults for xiuxian-qianji runtime.
# User overrides: $PRJ_CONFIG_HOME/xiuxian-artisan-workshop/qianji.toml

[llm]
model = "deepseek-chat"
base_url = "https://api.deepseek.com/v1"
api_key_env = "DEEPSEEK_API_KEY"

[memory_promotion.wendao]
graph_scope = "qianji:memory_promotion"
graph_dimension = 1024
persist = true
persist_best_effort = true

[checkpoint]
valkey_url = "redis://127.0.0.1:6379/0"

[workflow_state]
# Leave `local_duckdb_path` unset so runtime-owned local state defaults to
# `$PRJ_DATA_HOME/xiuxian-qianji`. Operator deployments may override this value
# in their root `qianji.toml`.

[server]
bind_addr = "127.0.0.1:38130"
flight_bind_addr = "127.0.0.1:38131"
require_valkey_ready = false
