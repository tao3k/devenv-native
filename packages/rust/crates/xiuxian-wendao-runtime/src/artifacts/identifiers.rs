//! Named identifier surfaces for runtime artifact helpers.

/// Borrowed artifact identifier accepted by runtime artifact helper APIs.
pub type RuntimeArtifactIdRef<'a> = &'a str;
/// Borrowed plugin identifier accepted by runtime artifact helper APIs.
pub type RuntimePluginIdRef<'a> = &'a str;
