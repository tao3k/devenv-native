[package]
name = "xiuxian-memory"
version.workspace = true
edition.workspace = true
description = "MemRL - Self-evolving episodic memory via runtime Q-Learning"
authors = ["CyberXiuXian Artisan workshop"]
license = "Apache-2.0"
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
rust-lang-project-harness = { workspace = true }

[lints]
workspace = true
